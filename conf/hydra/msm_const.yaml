run:
  dir: logs/${experiment.name}/${now:%Y-%m-%d_%H-%M-%S}/run/${model.loss}
sweep:
  dir: logs/${experiment.name}/${now:%Y-%m-%d_%H-%M-%S}/multirun/${model.loss}
  # ${now:%Y-%m-%d_%H-%M-%S}
  # subdir: ${hydra.job.num}